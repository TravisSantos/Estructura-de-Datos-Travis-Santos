let numeros = [1, 2, 3, 4, 5, 6];

// Imprimir los números usando un ciclo for
for (let i = 0; i < numeros.length; i++) {
    console.log(numeros[i]);
} numeros[1], numeros[2], numeros[3], numeros[4], numeros[5];