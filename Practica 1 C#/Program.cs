using System;

class Program
{
    static void Main()
    {
        // Declarar el array
        int[] numeros = { 1, 2, 3, 4, 5, 6 };

        // Imprimir todo el array 
        Console.WriteLine(string.Join(", ", numeros));
    }
}